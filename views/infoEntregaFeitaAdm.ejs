<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Informações da entrega</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        html, body {
            height: 100%;
            margin: 0;
            font-family: 'Roboto', sans-serif;
            display: flex;
            flex-direction: column;
            background-color: #193361;
            color: white;
        }

        .container {
            margin: 20px auto;
            padding: 20px;
            color: black;
            background-color: aliceblue;
            border-radius: 10px;
            max-width: 600px; /* Defina uma largura máxima para responsividade */
            width: 90%; /* Largura de 90% para telas pequenas */
        }

        .titulo {
            text-align: center;
            margin-top: 25px;
        }

        .titulo h1 {
            margin: 0;
            color: #ffffff;
        }

        .form-container {
            display: flex;
            flex-direction: column;
            align-items: stretch; /* Alinhar os itens para preencher a largura */
            gap: 20px;
        }

        .voltar-adicionar {
            display: flex;
            justify-content: center;
            gap: 10px;
            align-items: center;
        }

        .voltar-link {
            text-decoration: none;
            color: white;
            font-size: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #004080;
            padding: 10px;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.3s ease;
            border: 2px solid transparent;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 120px;
            text-align: center;
        }

        .voltar-link:hover {
            background-color: #003366;
            transform: scale(1.05);
        }

        .container h2 {
            color: #003366;
            margin: 10px 0 0 10px;
            font-size: 20px;
        }

        .indicacao-detalhes {
            width: 100%;
            border-radius: 10px;
            background-color: #ffffff;
            padding: 7px;
            margin-bottom: 15px;
            box-sizing: border-box;
        }

        input[type="text"] {
            border: none;
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            box-sizing: border-box;
        }

        footer {
            text-align: center;
            color: #333;
            font-size: 0.9em;
            width: 100%;
            position: relative; /* Ajuste para manter o footer na posição correta */
            margin-top: auto;
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .voltar-link {
                font-size: 16px;
                padding: 8px;
                width: 110px;
            }
        }

        @media (max-width: 600px) {
            .voltar-link {
                font-size: 14px;
                padding: 6px;
                width: 100px;
            }
        }

        @media (max-width: 480px) {
            .voltar-link {
                font-size: 12px;
                padding: 5px;
                width: 90px;
            }
        }

        nav{

            position: relative;
            z-index: 1000;
        }
    </style>
</head>
<body>
    <header>
        <%- include('./navbar.ejs', { activePage: 'infoEntregaFeitaAdm' }) %> 
    </header>
    <div class="titulo">
        <h1>Informações da entrega</h1>
    </div>
    <div class="container">
        <div class="form-container">
            <form id="indicacaoForm">
                <h2>Data da entrega</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="data_entrega" id="data_entrega" placeholder="Data que foi entregue a camiseta" value="<%= entrega.data_entrega %>" readonly>
                </div>
                <h2>Camiseta</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="camiseta" id="camiseta" placeholder="Modelo da camiseta" value="<%= entrega.modelo_camiseta %>" readonly>
                </div>
                <h2>Nome</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="titulo" id="titulo" placeholder="Nome do destinatário" value="<%= entrega.nome %>" readonly>
                </div>
                <h2>E-mail</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="email" id="email" placeholder="E-mail do destinatário" value="<%= entrega.email %>" readonly>
                </div>
                <h2>CPF</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="cpf" id="cpf" placeholder="CPF do destinatário" value="<%= entrega.cpf %>" readonly>
                </div>
                <h2>Turma</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="turma" id="turma" placeholder="Turma do destinatário" value="<%= entrega.nome_turma %>" readonly>
                </div>
                <h2>Curso</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="curso" id="curso" placeholder="Curso do destinatário" value="<%= entrega.nome_curso %>" readonly>
                </div>
                <div class="voltar-adicionar">
                    <a href="/entregaFeitaAdm" class="voltar-link">
                        <i class="fa fa-arrow-left"></i> Voltar
                    </a>
                </div>
            </form>
        </div>
    </div>
    <footer>
        <%- include('./footer.ejs', { activePage: 'infoEntregaFeitaAdm' }) %>
    </footer>
</body>
</html>
