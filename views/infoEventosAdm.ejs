<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Informações do Evento</title>
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>

<body>
    <header>
        <%- include('./navbar.ejs', { activePage: 'infoEventosAdm' }) %>
    </header>

    <div class="titulo">
        <h1>Informações do Evento</h1>
    </div>

    <div class="container">
        <form action="/editarEventoAdm/<%= eventoSelecionado.cod_evento %>" method="post" id="eventoForm">
            <div class="form-container">
                <h2>Nome do Evento</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="nome_evento" id="nome_evento" placeholder="Nome do evento"
                        value="<%= eventoSelecionado.nome_evento %>" required>
                </div>

                <h2>Descrição</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="descricao_evento" id="descricao" placeholder="Descrição do evento"
                        value="<%= eventoSelecionado.descricao_evento %>" required>
                </div>

                <h2>Data do Evento</h2>
                <div class="indicacao-detalhes">
                    <input type="date" name="data_evento" id="data_ocorrencia"
                        value="<%= formatDate(eventoSelecionado.data_evento) %>" required>
                </div>

                <h2>Local do Evento</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="local_evento" id="local_evento" placeholder="Local do evento"
                        value="<%= eventoSelecionado.local_evento %>" required>
                </div>

                <div class="voltar-adicionar">
                    <a href="/eventosAdm" class="voltar-link">
                        <i class="fa fa-arrow-left"></i> Voltar
                    </a>
                    <button type="submit" class="voltar-link">Salvar</button>
                </div>
            </div>
        </form>

        <form action="/excluirEventoAdm/<%= eventoSelecionado.cod_evento %>" method="post" class="excluir-form">
            <button type="submit" class="voltar-link excluir">Excluir</button>
        </form>
    </div>

    <footer>
        <%- include('./footer.ejs', { activePage: 'infoEventosAdm' }) %>
    </footer>

    <% function formatDate(date) { 
        var d = new Date(date); 
        var year = d.getFullYear(); 
        var month = ('0' + (d.getMonth() + 1)).slice(-2); 
        var day = ('0' + d.getDate()).slice(-2); 
        return year + '-' + month + '-' + day; 
    } %>

        <style>
            html,
            body {
                height: 100%;
                margin: 0;
                font-family: 'Roboto', sans-serif;
            }

            body {
                display: flex;
                flex-direction: column;
                background-color: #193361;
                color: white;
            }

            .container {
                margin: 20px auto;
                padding: 20px;
                color: black;
                background-color: aliceblue;
                border-radius: 10px;
            }

            .titulo {
                text-align: center;
                margin-top: 20px;
            }

            .titulo h1 {
                margin: 0;
                color: #ffffff;
            }

            .form-container {
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                gap: 20px;
            }

            .voltar-adicionar {
                display: flex;
                gap: 10px;
                align-items: center;
            }

            .voltar-link {
                text-decoration: none;
                color: white;
                font-size: 18px;
                display: flex;
                align-items: center;
                background-color: #004080;
                padding: 10px 20px;
                border-radius: 5px;
                transition: background-color 0.3s ease, transform 0.3s ease;
                border: 2px solid transparent;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            }

            .voltar-link:hover {
                background-color: #003366;
                transform: scale(1.05);
            }

            .voltar-link.excluir {
                background-color: #cc0000;
            }

            .voltar-link.excluir:hover {
                background-color: #990000;
            }

            .fa-arrow-left,
            .fa-save {
                margin-right: 8px;
            }

            .container h2 {
                color: #003366;
                margin: 10px 0px 0px 10px;
                font-size: 20px;
            }

            .indicacao-detalhes {
                width: 100%;
                max-width: 700px;
                border-radius: 10px;
                background-color: #ffffff;
                padding: 10px;
                margin-bottom: 15px;
            }

            input[type="text"],
            textarea,
            select {
                border-style: none;
                width: calc(100% - 20px);
                padding: 10px;
                border-radius: 5px;
                resize: vertical;
            }

            textarea {
                height: 100px;
            }

            footer {
                text-align: center;
                color: #333;
                font-size: 0.9em;
                width: 100%;
                position: absolute;
                bottom: 0;
            }
        </style>
</body>

</html>