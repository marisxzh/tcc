<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solicitar Patrimônio</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <%- include('./navbarAluno.ejs', { activePage: 'livrosAluno' }) %> <!-- Navbar -->
    </header>

    <div class="titulo">
        <h1>Solicitar Patrimônio</h1> <!-- Título da página -->
    </div>

    <div class="container">
        <!-- Formulário para Adicionar Indicação -->
        <form action="/inserirSolicitacaoPatrimonioAluno" method="post">
            <!-- Campo oculto para armazenar o cod_usuario -->
            <input type="hidden" name="cod_usuario" value="<%= cod_usuario %>"> 

            <!-- Campo para o título do livro -->
            <h2>Patrimônio</h2>
            <select name="cod_patrimonio" required>
                <% patrimonio.forEach(patrimonio => { %> <!-- Use 'patrimonio' em vez de 'materiais' -->
                    <option value="<%= patrimonio.cod_patrimonio %>"><%= patrimonio.nome_patrimonio %></option>
                <% }) %>
            </select>
        

            <!-- Campo para o status da indicação -->
            <h2>Status da Indicação</h2>
            <div class="indicacao-detalhes">
                <option value="0">Solicitação pendente</option>
            </div>

            <!-- Botões de Voltar e Adicionar -->
            <div class="voltar-adicionar">
                <a href="/solicitacoesPatrimoniosAluno" class="voltar-link">
                    <i class="fa fa-arrow-left"></i> Voltar
                </a>
                <button type="submit" class="voltar-link">
                    <i class="fa fa-save"></i> Adicionar
                </button>
            </div>
        </form>
    </div>

    <!-- Rodapé -->
    <footer>
        <%- include('./footer.ejs', { activePage: 'livrosAluno' }) %>
    </footer>
</body>
</html>

<!-- Estilos para a página -->
<style>


nav{
            position: relative;
            z-index: 1000;
        }

html, body {
    height: 100%;
    margin: 0;
    font-family: 'Roboto', sans-serif;
}

body {
    display: flex;
    flex-direction: column;
    background-color: #003366;
    color: white;
}

.container {
    margin: 100px auto;
    margin-bottom: 200px;
    padding: 20px;
    color: black;
    background-color: aliceblue;
    border-radius: 10px;
    width: 80%;
    max-width: 700px; /* Máximo de 700px para telas grandes */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    max-height: 650px ;
    flex-grow: 1; /* Expande a altura da página para ocupar o espaço vazio */
}

.titulo {
    text-align: center;
    margin-top: 20px;
}

.titulo h1 {
    margin: 0;
    color: #ffffff;
}

.voltar-adicionar {
    margin: 50px 10px;
    display: flex;
    justify-content: center;
    gap: 20px;
}

.voltar-link {
    text-decoration: none;
    color: white;
    font-size: 18px;
    display: flex;
    align-items: center;
    background-color: #004080;
    padding: 10px 20px;
    border-radius: 5px;
    transition: background-color 0.3s ease, transform 0.3s ease;
    border: 2px solid transparent;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.voltar-link:hover {
    background-color: #003366;
    transform: scale(1.05);
}

.fa-arrow-left, .fa-save {
    margin-right: 8px;
}

.container h2 {
    color: #003366;
    margin: 10px 0px 0px 10px;
    font-size: 20px;
}

.indicacao-detalhes {
    border-radius: 10px;
    background-color: #ffffff;
    padding: 10px;
    margin-bottom: 15px;
    width: 100%; /* Largura completa em telas menores */
    box-sizing: border-box;
}

input[type="text"],
input[type="date"],
textarea,
select {
    border-style: none;
    width: calc(100% - 20px);
    padding: 10px;
    border-radius: 5px;
    resize: vertical;
}

textarea {
    height: 100px;
}

footer {
    text-align: center;
    color: #333;
    font-size: 0.9em;
    width: 100%;
    position: relative;
    bottom: 0;
    margin-top: auto; /* Isso garante que o footer fique no fim da página */
}

/* Media Queries para responsividade */

/* Para telas menores que 768px (tablets e celulares) */
@media (max-width: 768px) {
    .container {
        width: 90%; /* Mais espaço nas laterais em telas menores */
    }

    .voltar-link {
        font-size: 16px;
        padding: 8px 16px;
    }

    .container h2 {
        font-size: 18px;
        margin: 10px 0;
    }

    input[type="text"],
    input[type="date"],
    textarea,
    select {
        padding: 8px;
        width: calc(100% - 16px); /* Ajuste proporcional em dispositivos móveis */
    }

    textarea {
        height: 80px;
    }
}

/* Para telas menores que 480px (smartphones) */
@media (max-width: 480px) {
    .container {
        width: 95%; /* Quase toda a largura da tela */
    }

    .voltar-link {
        font-size: 14px;
        padding: 6px 12px;
    }

    .titulo h1 {
        font-size: 24px;
    }

    .container h2 {
        font-size: 16px;
    }

    input[type="text"],
    input[type="date"],
    textarea,
    select {
        padding: 6px;
        width: calc(100% - 12px); /* Ajuste proporcional em smartphones */
    }

    textarea {
        height: 70px;
    }
}
</style>