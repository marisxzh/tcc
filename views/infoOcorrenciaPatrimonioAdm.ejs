<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Informações da Ocorrência</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
</head>

<body>
    <header>
        <%- include('./navbar.ejs', { activePage: 'infoOcorrenciaPatrimonioAdm' }) %>
    </header>

    <div class="titulo">
        <h1>Informações da Ocorrência</h1>
    </div>

    <div class="container">
        <div class="form-container">
            <form id="ocorrenciaPatrimonioForm" action="/editarOcorrenciaPatrimonioAdm/<%= ocorrencia.cod_ocorrencia %>" method="post">
                <h2>Patrimonio</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="patrimonio" id="patrimonio" placeholder="Nome do patrimonio"
                        value="<%= ocorrencia.nome_patrimonio %>" readonly>
                </div>

                <h2>Data da ocorrência</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="data_ocorrencia" id="data_ocorrencia"
                        placeholder="Data em que aconteceu a ocorrência"
                        value="<%= ocorrencia.data_ocorrencia %>" readonly>
                </div>

                <h2>Usuario que indicou a ocorrência</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="usuario" id="usuario" placeholder="Nome de quem indicou"
                        value="<%= ocorrencia.nome %>" readonly>
                </div>

                <h2>Detalhes</h2>
                <div class="indicacao-detalhes">
                    <input type="text" name="detalhe" id="detalhe" placeholder="Detalhe da ocorrência"
                        value="<%= ocorrencia.detalhes_ocorrencia %>" readonly>
                </div>

                <h2>Status</h2>
                <div class="indicacao-detalhes">
                    <select name="status" id="status" required>
                        <option value="" disabled>Selecione o status</option>
                        <option value="0" <%= ocorrencia.status_ocorrencia == 0 ? 'selected' : '' %>>Quebrado</option>
                        <option value="1" <%= ocorrencia.status_ocorrencia == 1 ? 'selected' : '' %>>Funcionando</option>
                        <option value="2" <%= ocorrencia.status_ocorrencia == 2 ? 'selected' : '' %>>Em Manutenção</option>
                    </select>
                </div>

                <div class="voltar-adicionar">
                    <a href="/ocorrenciasPatrimonioAdm" class="voltar-link">
                        <i class="fa fa-arrow-left"></i> Voltar
                    </a>
                    <button type="submit" class="voltar-link">Salvar</button>
                </div>
            </form>
        </div>
    </div>

    <footer>
        <%- include('./footer.ejs', { activePage: 'infoOcorrenciaPatrimonioAdm' }) %>
    </footer>
</body>

</html>

<style>
    html, body {
        height: 100%;
        margin: 0;
        font-family: 'Roboto', sans-serif;
    }

    body {
        display: flex;
        flex-direction: column;
        background-color: #193361;
        color: white;
    }

    .container {
        margin: 20px auto;
        padding: 20px;
        max-width: 800px;
        color: black;
        background-color: aliceblue;
        border-radius: 10px;
        flex: 1; /* Adiciona flexibilidade para que o conteúdo ocupe o espaço disponível */
    }

    .titulo {
        text-align: center;
        margin-top: 25px;
    }

    .titulo h1 {
        margin: 0;
        color: #ffffff;
    }

    .form-container {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .voltar-adicionar {
        display: flex;
        gap: 10px;
        align-items: center;
        justify-content: center;
    }

    .voltar-link {
        text-decoration: none;
        color: white;
        font-size: 18px;
        display: flex;
        align-items: center;
        background-color: #004080;
        padding: 10px 20px;
        border-radius: 5px;
        transition: background-color 0.3s ease, transform 0.3s ease;
        border: 2px solid transparent;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    }

    .voltar-link:hover {
        background-color: #003366;
        transform: scale(1.05);
    }

    .container h2 {
        color: #003366;
        margin: 10px 0px 0px 10px;
        font-size: 20px;
    }

    .indicacao-detalhes {
        width: 100%;
        max-width: 700px;
        border-radius: 10px;
        background-color: #ffffff;
        padding: 7px;
        margin-bottom: 15px;
    }

    input[type="text"], textarea, select {
        border-style: none;
        width: calc(100% - 20px);
        padding: 10px;
        border-radius: 5px;
        resize: vertical;
    }

    footer {
        text-align: center;
        color: #333;
        font-size: 0.9em;
        width: 100%;
        margin-top: auto; /* Garante que o footer fique no fim da página */
    }

    /* Responsividade */
    @media (max-width: 768px) {
        .container {
            padding: 10px;
            width: 90%;
        }

        .voltar-link {
            padding: 10px 15px;
            font-size: 16px;
        }

        .titulo h1 {
            font-size: 24px;
        }

        .container h2 {
            font-size: 18px;
        }

        input[type="text"], textarea, select {
            width: calc(100% - 10px);
        }
    }

    @media (max-width: 480px) {
        .voltar-adicionar {
            flex-direction: column;
            gap: 10px;
        }

        .voltar-link {
            width: 100%;
            justify-content: center;
        }

        .indicacao-detalhes {
            width: 100%;
        }

        textarea {
            height: 80px;
        }
    }

    nav {
        position: relative;
        z-index: 1000;
    }
</style>
