<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Material</title>
    <!-- Link para ícones do Font Awesome -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <link rel="icon" href="./img/favicon-32x32.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            margin: 0;
            font-family: 'Roboto', sans-serif;
            background-color: #003366;
            color: white;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .titulo {
            text-align: center;
            margin-top: 20px;
        }

        .titulo h1 {
            margin: 0;
            color: #ffffff;
        }

        .voltar {
            margin: 20px 10px;
            display: flex;
            justify-content: center;
        }

        .voltar-link {
            text-decoration: none;
            color: white;
            font-size: 18px;
            display: flex;
            align-items: center;
            background-color: #004080;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.3s ease;
            border: 2px solid transparent;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .voltar-link:hover {
            background-color: #003366;
            transform: scale(1.05);
        }

        .fa-arrow-left {
            margin-right: 8px;
        }

        .container {
            margin: 20px auto;
            padding: 20px;
            max-width: 800px; /* Largura máxima da caixa */
            color: black;
            background-color: aliceblue;
            border-radius: 10px;
            width: 100%; /* Largura padrão */
            transition: width 0.3s ease; /* Transição suave */
        }

        /* Aumentando gradualmente o tamanho da caixa conforme a tela aumenta */
        @media (min-width: 480px) {
            .container {
                width: 90%; /* 90% da largura da tela em telas pequenas */
            }
        }

        @media (min-width: 768px) {
            .container {
                width: 80%; /* 80% da largura da tela em telas médias */
            }
        }

        @media (min-width: 992px) {
            .container {
                width: 70%; /* 70% da largura da tela em telas grandes */
            }
        }

        .container h2 {
            color: #003366;
            margin: 10px 0;
            font-size: 20px;
        }

        .material-nome,
        .material-descricao {
            padding: 10px;
            margin: 10px 0;
            border-radius: 10px;
            background-color: #ffffff;
        }

        .material-nome h3,
        .material-descricao h3 {
            margin: 0;
            color: #003366;
        }

        footer {
            text-align: center;
            color: #333;
            font-size: 0.9em;
            width: 100%;
            position: absolute;
            bottom: 0;
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .voltar-link {
                font-size: 16px;
                padding: 8px 16px;
                width: 80%;

            }

            .container {
                padding: 15px;
                width: 90%;
            }

            .container h2 {
                font-size: 18px;
            }

            .material-nome,
            .material-descricao {
                width: 100%;
            }
        }

        @media (max-width: 480px) {
            .voltar-link {
                font-size: 14px;
                padding: 6px 12px;
            }

            .container {
                padding: 10px;
                width: 90%;
            }

            .container h2 {
                font-size: 16px;
            }
        }
        nav{
            position: relative;
            z-index: 1000;
        }

    </style>
</head>

<body>
    <header>
        <%- include('./navbarAluno.ejs', { activePage: 'materiaisAluno' }) %>
    </header>

    <div class="titulo">
        <h1>Detalhes do Material</h1>
    </div>

    <!-- Detalhes do material -->
    <div class="container">
        <h2>Material</h2>
        <div class="material-nome">
            <h3>
                <%= material.nome_material %>
            </h3>
        </div>
        <h2>Descrição</h2>
        <div class="material-descricao">
            <h3>
                <%= material.descricao_material %>
            </h3>
        </div>
        <!-- Ícone de seta para voltar -->
        <div class="voltar">
            <a href="/materiaisAluno" class="voltar-link">
                <i class="fa fa-arrow-left"></i> Voltar
            </a>
        </div>
    </div>

    <!-- Rodapé do site -->
    <footer>
        <%- include('./footer.ejs', { activePage: 'materiaisAluno' }) %>
    </footer>
</body>

</html>